export interface ChatData{
    id: number;
    label: string;
    max: number;
    used: number;
}

export interface BarMeasures {
    label: string;
    used :string;
    available :string;
    overload :string;
    whiteSpace :string;
  }